[project]
name="call_me_maybe"
version="1.0"
dependencies = [
    "numpy>=2.2.6",
    "mypy",
    "flake8",
    "pydantic>=2.13.4",
    "llm-sdk",
    "torch>=2.12.0",
    "accelerate>=1.13.0",
]

[tool.mypy]
exclude = ["src/llm_sdk"]